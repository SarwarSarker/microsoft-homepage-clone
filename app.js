const show = document.querySelector('.show');
const menubtn = document.querySelector('.menu-btn');
const menubar = document.querySelector('.main-menu');

menubtn.addEventListener('click', () => menubar.classList.toggle('show'))

